export class Address {
    constructor(public road: String, public postalCode: Number, public city: String) {
        this.road = road;
        this.postalCode = postalCode;
        this.city = city;
    }
}